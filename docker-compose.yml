services:
  app:
    build: .
    container_name: rss-aggregator
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./app:/app/app
      - ./Main Architechture:/app/Main Architechture
    env_file:
      - .env
    restart: unless-stopped
